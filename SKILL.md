---
name: causal-inference-reader
description: >-
  为《因果推理深度读本》提供领域专用的 AI 编辑与核验约束。用于章节修订、概念澄清、论证检查、文献补强、术语统一和成品验收；重点防止因果问题、目标量、识别、估计、解释和迁移之间的概念错位，并保持全书既定结构与 LaTeX 工程约定。
---

# 因果推理深度读本：领域编辑 Skill

## 定位

这是一个领域专用 Skill，不是通用 Agent 工作流。通用的范围控制、工具使用、证据诚实和完成报告应由上层系统、Harness 或通用 Skill 负责；本文件只保留因果推理教材中特别容易出错、且会实质影响书稿质量的约束。

不要把本文件当作穷尽性 checklist。若实际章节中更重要的问题没有列在这里，应优先处理真实问题。

## 项目不变量

- `main.tex` 是唯一编译入口。
- 全部正文位于 `chapters/`。
- 文献只维护在 `references.bib`。
- 新术语首次出现时按现有约定使用 `\term{}`。
- 默认保持“3 个主部、11 章正文、4 个附录”的结构。
- 不把本书扩展成统计证明手册、编程教材、软件教程或完整哲学史，除非用户明确要求。
- 比较性附录中的中国哲学、自由意志和量子因果不应被表述为现代因果推断主线的历史先声。
- 工程层最终以 `make check` 为验收标准。

## 核心因果边界

### 1. 区分因果问题、目标量、识别与估计

技术段落应在需要时区分：

- **因果问题**：真正要回答的干预、反事实、机制或迁移问题；
- **目标量（estimand）**：要定义的数学对象；
- **识别假设**：为什么数据与设计足以确定目标量；
- **识别表达式**：目标量如何化为可观测分布的函数；
- **估计**：有限样本中如何近似该表达式并量化不确定性；
- **解释与外推**：结果适用于什么总体、环境和机制条件。

识别不等于估计；预测准确不证明因果识别；模型拟合良好不证明因果假设成立。

### 2. 保持潜在结果与 SCM 的语义边界

潜在结果与结构因果模型可以互相映照，但不要强行写成完全同义体系。

特别注意：

- 一致性、可交换性、正值性等是具有具体语义的识别条件，不应含混写成“数据满足因果条件”；
- DAG 与结构方程编码研究者接受的结构假设，不是从相关性中自动读出的现实结构；
- 后门准则、前门准则和 do-calculus 是识别工具，不是估计器；
- 图上路径的缺失不自动证明现实机制不存在；
- 反事实、干预和观察条件概率不要相互替代。

### 3. 主动防止高频概念错误

遇到相关内容时重点检查：

- 把相关性直接升级为因果关系；
- 把“控制更多变量”写成普遍更安全；
- 忽略碰撞点、选择偏差、中介调整或处理后变量问题；
- 把随机化写成有限样本协变量完全平衡；
- 把无混杂当成可由观测数据本身证实的事实；
- 忽略正值性的目标总体和支持范围含义；
- 混淆工具变量的相关性、独立性与排除限制；
- 把 RDD、DiD、IV 等设计直接写成“天然因果”而省略识别条件；
- 把因果发现算法输出的单图或方向当作已证实真实结构；
- 把算法稳定性当作因果正确性；
- 混淆 transportability、generalizability、external validity 与普通 domain adaptation；
- 把 causal machine learning 缩减为“用机器学习预测处理效应”，或把 CATE 估计误写成个体确定效应恢复。

## 深化原则

“深化”优先表现为：

- 增加一个真正必要的概念区分；
- 补足识别假设与结论之间缺失的推理桥梁；
- 加入能推翻错误直觉的最小失败案例；
- 说明方法的适用边界、竞争解释或不可识别部分；
- 修复前后章节之间会造成误解的接口。

不要把深化等同于增加篇幅、人物、算法名称或引用数量。已有内容正确且教学充分时，不为显示修改而重写。

## 文献与归因

- 核心事实、历史归属、理论归属和方法来源优先核对原始论文、原著或正式技术来源。
- 二手综述可用于背景与争议，但不要冒充原作者原话。
- 新增引用前先检查 `references.bib` 是否已有相同来源或合适条目。
- 不凭模型记忆生成页码、引文、DOI 或不存在的文献。
- 直接引文只有在能够核对原文时使用；否则准确转述。
- “未找到证据”不等于“没有研究”或“已经被否定”。
- 满足现有引用数量后，不为提高表面权威性机械堆叠文献。

## 修改时的领域检查

修改单章时，按实际需要同时检查：

- 目标段落在本章中的论证位置；
- 前后章节是否已经定义或限定同一概念；
- 符号、目标量与因果语义是否前后一致；
- 新增或变更引用是否与 `references.bib` 闭合；
- 新术语、标签、交叉引用和 LaTeX 命令是否符合现有约定。

只有当这些检查与当前修改有关时才执行；不要机械遍历全书。

## 完成标准

一次领域修改应同时满足：

- 实质问题已解决，而非只做语言润色；
- 结论强度没有超过识别假设真正支持的范围；
- 重要事实与理论归属有相称依据；
- 没有为追求完整感而无必要扩大篇幅或结构；
- 与全书术语、书目和 LaTeX 约定一致；
- `make check` 已通过，或明确说明尚未完成本地工程验证。
